/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* If you use images from external sources (like unsplash), 
     uncomment and add domains here:
  */
  // images: {
  //   domains: ['images.unsplash.com'],
  // },
};

export default nextConfig;